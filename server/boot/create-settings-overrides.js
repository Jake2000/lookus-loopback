var debug = require('debug')('boot:create-settings-overrides');
var _ = require('lodash');

module.exports = function(app) {
  //var settingsProto = app.models.usersetting.prototype;
  //var userIdSetter = Object.getOwnPropertyDescriptor(settingsProto, "user_id").set;
  //Object.defineProperty(settingsProto, 'user_id', {
  //  set: function(value) {
  //    //if(!settingsProto.id)
  //      userIdSetter.call(settingsProto, value);
  //  }
  //});
};
